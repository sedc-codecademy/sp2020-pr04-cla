class newAd {
    constructor({ id, adCategory, adTitle, adType, adAmount, adDescription }) {
        this.id = id
        this.adCategory = adCategory
        this.adTitle = adTitle
        this.adType = adType
        this.adAmount = adAmount
        this.adDescription = adDescription
    }
}

module.exports = newAd;
